<%= form_for like, html: {method: :delete} do |f| %>
  <div><%= hidden_field_tag :id, like.id %></div>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= f.submit "Unlike this post", class: "btn" %>
<% end %>
