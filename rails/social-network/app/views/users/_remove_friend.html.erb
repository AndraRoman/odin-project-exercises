<% friendship = current_user.relationship(user) %>
<%= form_for friendship, html: {method: :delete} do |f| %>
  <div><%= hidden_field_tag :id, user.id %></div>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= f.submit "Unfriend", class: "btn" %>
<% end %>
