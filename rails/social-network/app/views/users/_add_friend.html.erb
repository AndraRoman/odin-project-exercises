<%= form_for(current_user.active_friendships.build) do |f| %>
  <div><%= hidden_field_tag :recipient_id, user.id %></div>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= f.submit "Send friend request", class: "btn" %>
<% end %>
